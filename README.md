# Ubuntu 16.04.2 LTS configuration files

---

## zsh

Run:
    ```sh
    apt-get install zsh
    apt-get install git-core
    sudo apt install fonts-powerline
    ```

Customize:
    ```
    .zshrc
    ```

---
